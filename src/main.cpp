


int main()
{
  // set input message
  // encode pt_z -> need encoding!

  // get dft matrix -> need dft matrix!
  // encode matrix into ptxts -> need encoding matrix in ptxts!
  // lin transform on ptxt side
  // (do lin transform few times)

  // decode back to msg
  // measure error

  // compute matrix norm
  // compute expected norm

  // compute vector norm
}
